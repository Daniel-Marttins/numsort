spring.application.name=numsort

# Configura��es do DataSource (conex�o com o PostgreSQL)
spring.datasource.url=jdbc:postgresql://localhost:5432/numsort
spring.datasource.username=postgres
spring.datasource.password=Utkz1998

# Configura��o do driver JDBC
spring.datasource.driver-class-name=org.postgresql.Driver

# Dialeto SQL espec�fico para PostgreSQL (ajuda o Hibernate a gerar consultas compat�veis com o banco)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Definir a estrat�gia de cria��o de schema (update, create, create-drop, validate, none)
spring.jpa.hibernate.ddl-auto=update

# Mostrar as consultas SQL no console (opcional)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
